# Google may crawl everything, the forum included, and its AI may use what
# it fetched. Every other bot stays out of the forum and its API, the worker
# request budget is not for crawlers. The rest of the site remains open to
# all bots by design.

User-agent: Googlebot
Allow: /

# Not a crawler, a use-permission token: this lets Google's AI (Gemini, the
# AI Overviews in search) draw on what Googlebot already fetched, forum and
# all. It adds no requests of its own.
User-agent: Google-Extended
Allow: /

# The board's threads have their own URLs now (/t/<id>-<slug>, served by the
# worker with the thread's text in the page). They are the worker's requests to
# pay for, so the same rule holds: Google may read them, every other bot stays
# out of the forum, its feeds and its sitemap.
User-agent: *
Disallow: /community.html
Disallow: /t/
Disallow: /sitemap-threads.xml
Disallow: /api/

Sitemap: https://merecatholicity.com/sitemap.xml
Sitemap: https://merecatholicity.com/sitemap-threads.xml
